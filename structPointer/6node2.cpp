/** 
 * Node lecture 2 
 *  Create a function (Take current node and value as input ) which will: 
 * 1. create a node                     node *n;    n=new node;
 * 2. assign it a value                 n->val = val; n->next = NULL
 * 3. link with current node            current-> next = n;
 * 4. return node address               return n;
 * 
 * */
